server:
  port: 8082

spring:
  application:
    name: cars-service
  datasource:
    url: jdbc:mysql://localhost:3308/carsdb
    username: carsuser
    password: carspwd
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
