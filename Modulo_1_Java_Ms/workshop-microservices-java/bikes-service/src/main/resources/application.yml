server:
  port: 8081

spring:
  application:
    name: bikes-service
  datasource:
    url: jdbc:mysql://localhost:3307/bikesdb
    username: bikesuser
    password: bikespwd
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
